# transformer-image-captioning-flickr8k
Implemented a Transformer encoder-decoder for image captioning using PyTorch. Integrated ResNet-50 features with sequence modeling, applied positional encoding and attention mechanisms, and evaluated using BLEU score. Compared custom model with PyTorch Transformer and ViT-GPT2.
